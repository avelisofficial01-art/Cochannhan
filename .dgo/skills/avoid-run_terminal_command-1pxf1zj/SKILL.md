---
name: avoid-run_terminal_command-1pxf1zj
description: DO NOT retry — "Avoid: run_terminal_command with the same argument signature"
version: 1.0.0
metadata:
  dgo:
    kind: antipattern
    generated: true
    source: doom-loop-guard
    reason: "doom-loop guard fired 3× on run_terminal_command"
---

# ⚠️ Avoid: run_terminal_command with the same argument signature

> This SKILL was generated by DGOCoder when the doom-loop guard observed the same tool fail repeatedly with identical arguments. Treat it as a hard "do not repeat" signal.

## Why this is here
User intent: Continue development.

Before doing anything else:

1. Read AGENTS.md.
2. Read PROJECT_STATUS.md.
3. Read GAME_BUILD_PLAN.md.
4. Read CHANGELOG.md.

Determine the current sprint and current task.

Continue ONLY th
[truncated]

Tool: run_terminal_command
Args: {
  "command": "npm run typecheck",
  "timeoutMs": 60000
}

The doom-loop guard observed this exact call fail 3 times in a row.

## What to do instead
Do NOT retry "run_terminal_command" with the same arguments — the guard will block it again.
Diagnose why the call failed before changing strategy:
1. Read the failure message above. If it is a "Permission denied" / "Error: ..." pattern, the tool itself is rejecting the request — not a flaky network issue.
2. Switch approach: either re-scope the inputs (different path, narrower query, lower budget) or pick a different tool that achieves the same goal.
3. If the user is asking for something that genuinely needs this tool, ask one clarifying question instead of looping.

## Tools involved
- run_terminal_command

## Error excerpts captured before the block
- <terminal command="npm run typecheck" cwd="." exitCode="2" durationMs="4184">

## Tool argument signature (do not repeat verbatim)
```
run_terminal_command:{"command":"npm run typecheck","timeoutMs":60000}
```

## Bookkeeping
- Hits: 1
- Provider/model when triggered: custom/deepseek-v4-pro[1m]
- Last seen session: session-mr550ml6-7kwmvk8u
- Last updated: 2026-07-03T16:47:19.816Z
